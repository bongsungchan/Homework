import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.module(
    name: Module.coreDesignSystem.name,
    bundleId: Module.coreDesignSystem.bundleId,
    dependencies: []
)
